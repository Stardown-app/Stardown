{
  "name": "stardown",
  "version": "1.0.0",
  "description": "Copy a markdown link for the current page.",
  "scripts": {
    "firefox": "rollup --config rollup.config.firefox.mjs",
    "build": "npm install && npm run firefox && npm run assertClean && rm -r src",
    "update": "git restore . && git pull && npm run build",
    "assertClean": "git update-index --refresh && git diff-index --quiet HEAD --"
  },
  "devDependencies": {
    "rollup": "^4.18.0",
    "rollup-plugin-copy": "^3.5.0",
    "rollup-plugin-delete": "^2.0.0"
  }
}
